import os
def login():
    username = "{0.__init__.__globals__}"
    # username = "{0.__init__.__globals__[get_flag]}"
    res_username = ""
    ip = "140.112.196.161"
    if not res['username']:
        return 'user not found'

    if res[password] == password:
        return f'Hello {res["username"]} ｡:.ﾟヽ(*´∀`)ﾉﾟ.:｡'
    else:
        s = ('Wrong password for ' + username).format(res_username) + f', login from {ip}.'
        print(s)

